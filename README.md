# 💕 Валентинка — сайт ко Дню Святого Валентина

Романтический сайт-приглашение: слайды с фотографиями, вопрос «Будешь ли ты моей валентинкой?» и план свидания.

## Как это работает

1. **Слайды** — свайп влево или клик по карточкам, под каждой подпись
2. **Вопрос** — появляется после последнего слайда с кнопками «Да» и «Нет»
3. **«Да»** — взрыв из эмодзи и план свидания
4. **«Нет»** — кнопка убегает; после нескольких попыток — предупреждение

---

## ⚙️ Настройка под себя

Все настройки находятся в файле **`src/config.js`**. Его можно полностью отредактировать, не трогая остальной код.

### Страница

```js
PAGE: {
  title: 'Заголовок вкладки браузера',
}
```

### Фотографии (слайды)

```js
SLIDES: [
  { id: 0, src: 'ссылка-на-картинку.jpg', alt: 'Описание' },
  { id: 1, src: '...', alt: '...' },
  // Добавьте или удалите слайды — количество любое
]
```

- **src** — ссылка на изображение (URL) или путь к файлу в проекте, например `/images/photo1.jpg`
- **alt** — текст, если картинка не загрузится
- **id** — уникальный номер (0, 1, 2, …)

Для локальных картинок создайте папку `public/images`, положите туда файлы и укажите `src: '/images/имя-файла.jpg'`.

### Тексты

| Параметр | Описание |
|----------|----------|
| `question` | Основной вопрос («Будешь ли ты моей валентинкой?») |
| `yesButton` | Текст кнопки «Да» |
| `noButton` | Текст кнопки «Нет» |
| `slideHints` | Подписи под слайдами (по одной на каждый слайд) |
| `wrongSwipeHint` | Сообщение при свайпе вправо («Не в ту сторону») |
| `warningMessage` | Текст предупреждения при многократном «Нет» |
| `warningButton` | Кнопка закрытия предупреждения («Понятно») |
| `datePlanTitle` | Заголовок блока с планом свидания |
| `datePlanSignature` | Подпись внизу («С любовью» и т.п.) |
| `datePlanItems` | Пункты плана свидания (массив строк) |

### Эмодзи для анимации

```js
KISS_EMOJIS: ['😘', '💋', '❤️', '💕', '💗', '🥰', '😍']
```

Их можно заменить любыми другими эмодзи.

### Поведение

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `lastSlideDelaySeconds` | Сколько секунд показывать последний слайд | 5 |
| `warningAfter` | После скольких «Нет» показывать предупреждение | 4 |
| `warningOverlayDelay` | Задержка (мс) перед возможностью закрыть предупреждение кликом по фону | 500 |

---

## Запуск проекта

```bash
# Установка зависимостей
npm install

# Локальный запуск
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр сборки
npm run preview
```

Сайт будет доступен по адресу http://localhost:5173

---

## Деплой на Vercel

1. Залить проект в GitHub
2. Зайти на [vercel.com](https://vercel.com) и импортировать репозиторий
3. Нажать **Deploy**

Или через CLI:

```bash
npm i -g vercel
vercel
```

---

## Структура проекта

```
├── src/
│   ├── config.js       ← Здесь все настройки
│   ├── App.jsx
│   ├── main.jsx
│   ├── index.css
│   └── components/
│       ├── SlidesSection.jsx   (слайды)
│       ├── QuestionSection.jsx (вопрос и кнопки)
│       └── KissesSection.jsx   (взрыв и план)
├── public/             (для своих картинок)
├── index.html
└── package.json
```

---

## Советы

- Если добавили слайды — добавьте столько же подписей в `slideHints`
- Для картинок можно использовать [Imgur](https://imgur.com), [Cloudinary](https://cloudinary.com) или папку `public`
- Эмодзи можно вставлять в любой текст
- Если план свидания длинный — добавьте больше пунктов в `datePlanItems`
